{
  "name": "proxies-aio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "proxy:add": "node scripts/add-proxy.js",
    "add-proxy": "node scripts/add-proxy.js",
    "test": "node --test",
    "check": "node --test && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
